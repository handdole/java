package 부품만들기;

public class BongABbang {
		String inside ;     // 멤버 변수 생성
		int price;         // 멤버  변수 생성
		
		public void eat() {
			System.out.println(inside+" 붕어빵의 꼬리부터 먹습니다.");    // 멤버 메소드 //먹기 메소드 호출시 (inside 변수)붕어빵 꼬리부터 먹습니다 출력
		}
		public void takeout() {
			System.out.println(inside+" 붕어빵을 포장해 갑니다.");      // 멤버 메소드 // 포장 메소드 호출시 (inside 변수)붕어빵  포장해갑니다 출력
		}
	
	

}
